Place your executable files here:

Required executables:
- whisperfile.exe (Windows) or whisperfile (Linux/macOS)
- llamafile.exe (Windows) or llamafile (Linux/macOS)

Download from:
- Whisperfile: https://huggingface.co/Mozilla/whisperfile
- Llamafile: https://github.com/Mozilla-Ocho/llamafile

Make sure to make them executable on Linux/macOS:
chmod +x whisperfile
chmod +x llamafile
